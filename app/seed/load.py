"""Seed loader: a stub until the models exist.

The entrypoint runs this on every container start, so it has to exit 0.
"""


def main() -> int:
    print("no seed yet")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
